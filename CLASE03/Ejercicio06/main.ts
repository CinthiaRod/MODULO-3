import { Geometry } from "./geometry";

console.log(`El área del círculo con un radio de 3 es: ${Geometry.areaOfCircle(3)}`);
console.log(`El área del cuadrado con lado 5 es: ${Geometry.areaOfSquare(5)}`);
